import AddressForm from "./AddressForm";

function Address() {
  const handleAddress = () => {
    // TODO: save address
  };

  return <AddressForm onSubmit={handleAddress} />;
}

export default Address;
